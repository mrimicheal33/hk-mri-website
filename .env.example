# Optional — if set, uses Resend instead of FormSubmit for delivery
# RESEND_API_KEY=re_xxxxxxxx
# RESEND_FROM="HK MRI Instrument <info@mti-mill.com>"

# FormSubmit (default): first inquiry triggers a one-time activation email to
# michael.chan@mti-mill.com — click the link to enable delivery.
